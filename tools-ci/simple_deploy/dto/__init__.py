"""Внешние объекты передачи данных для интерфейсов simple-deploy.

DTO-модули описывают JSON/API-контракты отдельно от строк SQLite,
dataclass-записей состояния, внутренних read models и доменных entities. Это
слой, который можно безопасно показывать FastAPI response_model, web UI или
будущим интеграциям.

Если нужно изменить внутреннюю сборку state snapshot, сначала меняется
``models``/``registry``. DTO стоит менять только когда сознательно меняется
публичная форма ответа API.
"""
